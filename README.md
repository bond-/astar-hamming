astar-hamming
=============
To run the program:
* Clone the repo
* Browse to the root of this repo and run either of the below depending on your operating system
** `./gradlew run` (or)
** `./gradlew.bat run`